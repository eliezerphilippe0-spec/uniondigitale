# Bac à sable — Preview de test

Branche **jetable** (`preview/reloadly-sandbox`) servant uniquement à obtenir un
déploiement **Preview** Vercel (identique à `main`) pour tester le parcours
Reloadly sandbox (`/admin` → synchro catalogue, `/rechaj` → achat de test) sans
exposer le site de production.

À supprimer après le test. Ne rien fusionner depuis cette branche.
